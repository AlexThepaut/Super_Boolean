{
  "name": "superboolean",
  "version": "1.0.0",
  "description": "Module for dealing with MOTHERFUCKING boolean",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "super",
    "boolean"
  ],
  "author": "THEP, ",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^2.3.2"
  }
}
